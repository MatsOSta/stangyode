{
  "name": "stangyode",
  "version": "1.0.0",
  "private": true,
  "description": "The Stangyode company homepage.",
  "scripts": {
    "start": "node server.mjs",
    "build:atlas": "node atlas/scripts/build-atlas.mjs",
    "validate:atlas": "node atlas/scripts/validate-atlas.mjs",
    "check": "node --check public/translations.js && node --check public/app.js && node --check public/ai-engineering/atlas.js && npm run validate:atlas"
  }
}
